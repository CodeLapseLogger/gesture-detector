import { GestureDetectionStartMessageContainer } from "./styledComponents";

const GestureDetectionStartMessage = (props) => {
  return (
    <GestureDetectionStartMessageContainer>
      Please click on the button to start practicing hand gestures
    </GestureDetectionStartMessageContainer>
  );
};

export default GestureDetectionStartMessage;
